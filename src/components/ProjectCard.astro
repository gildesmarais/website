---
import Highlights from "./Highlights.astro"

export interface Props {
  title: string
  status: string
  description: string
  highlights?: Array<{ term: string; definition: string }>
  links?: Array<{ text: string; url: string; external?: boolean }>
}

const { title, status, description, highlights, links } = Astro.props

if (title.includes(" ")) {
  throw new Error(
    "Project title must be a single word (for slug/ID purposes). TODO: Improve heading id generation.",
  )
}
---

<article class="project-card">
  <header>
    <h2 class="accent" id={title.toLowerCase()}>
      {title}
      <small aria-label="project status">— {status}</small>
    </h2>
    <p class="lead">{description}</p>
  </header>

  <slot />

  {
    highlights && (
      <>
        <h3>Highlights</h3>
        <Highlights highlights={highlights} />
      </>
    )
  }

  {
    links && (
      <>
        <h3>Links</h3>
        <ul>
          {links.map(({ text, url, external }) => (
            <li>
              <a
                href={url}
                target={external ? "_blank" : undefined}
                rel={external ? "external nofollow noopener" : undefined}
              >
                {text}
              </a>
            </li>
          ))}
        </ul>
      </>
    )
  }
</article>

<style>
  .project-card {
    padding: 0;
    background-color: unset;
  }

  .project-card header > h2 small {
    font-weight: 500;
    opacity: 0.7;
  }

  .project-card h3 {
    font-size: 0.9em;
    color: var(--pico-muted-color);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    padding-left: 0;
  }

  .project-card a {
    color: var(--pico-color);
    text-decoration: none;
    letter-spacing: 0.02em;
  }
</style>
