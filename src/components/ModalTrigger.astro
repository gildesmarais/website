---
interface Props {
  imdbId: string
  title: string
  year: number
}

const { imdbId, title, year } = Astro.props
---

<button class="imdb-link-button" data-imdb-id={imdbId} aria-label={`More details for ${title}`}>
  {title} ({year})
</button>
